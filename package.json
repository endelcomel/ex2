{
  "name": "sqlite-api",
  "version": "1.0.0",
  "description": "A serverless API to access SQLite databases hosted on Netlify",
  "main": "functions/api.js",
  "scripts": {
    "start": "node functions/api.js",
    "build": "echo 'No build step required'"
  },
  "dependencies": {
    "express": "^4.18.2",
    "serverless-http": "^3.2.0",
    "sqlite3": "^5.1.6"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=14.0.0"
  }
}
